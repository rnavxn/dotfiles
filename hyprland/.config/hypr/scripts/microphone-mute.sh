#!/bin/bash
# export PATH=/usr/bin:/bin
# /usr/bin/pamixer --default-source --toggle-mute
# IS_MUTED=$(/usr/bin/pamixer --default-source --get-mute)
# if [ "$IS_MUTED" = "true" ]; then
#     notify-send "Microphone" "🔇 Muted"
# else
#     notify-send "Microphone" "🎤 Unmuted"
# fi
notify-send "Hi, its working!"
