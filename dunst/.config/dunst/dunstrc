[global]
# General
origin = top-right          # Position
offset = (20,20)            # horizontal offset from left, vertical from top
width = 300
height = (0, 300)
horizontal_padding = 10     # Padding between text and edges
padding = 10                 # Padding between text and edges (vertical)
text_icon_padding = 0
enable_posix_regex = true
word_wrap = yes
follow = mouse                      # Notification appears on the screen which has the cursor in multiple monitors

# Notification management
notification_limit = 7             # Number of notifications can appear in single set
gap_size = 7                        # Set gap size between notifications
frame_width = 0                     # Notification frame width, set 0 to disable
sort = true
hide_duplicate_count = true         # Shows duplicate message count if false
indicate_hidden = true              # Shows the remaining count of notifications yet to display when notification limit reached
show_indicators = false             # Shows indicatior for notifications with action
show_age_threshold = -1
sticky_history = true
history_length = 15

# Progress Bar
progress_bar = true
progress_bar_horizontal_alignment = center
progress_bar_height = 7
progress_bar_min_width = 150
progress_bar_max_width = 300
progress_bar_frame_width = 0
progress_bar_corner_radius = 3
progress_bar_corners = all                  # Define which corners to round when drawing the progress bar.
highlight = "#2E344050, #f94949ff"

# Text
font = Inter 10
markup = full
format = "<span size='larger'><b>%s</b></span>\n%b"     # Shows only Summary and Body
vertical_alignment = center
line_height = 0
foreground = "#ECEFF4ff"

# Icons
icon_position = left
max_icon_size = 48
min_icon_size = 24
enable_recursive_icon_lookup = false
icon_path = /usr/share/icons/char-white/status/48:/usr/share/icons/char-white/apps/48:/usr/share/icons/char-white/devices/48:/usr/share/icons/char-white/mimetypes/48:/usr/share/icons/char-white/actions/24:/usr/share/icons/char-white/places/48
icon_corner_radius = 0            # The corner radius of the icon image in pixels. Gives the icon rounded corners. Set to 0 to disable.

[urgency_low]
background = "#2E344070"        # nordic darkish (from Nord palette)
frame_color = "#2E344050"       # light border test
timeout = 5

[urgency_normal]
background = "#2E344070"        # slightly lighter Nordic
frame_color = "#3B425250"
timeout = 10

[urgency_critical]
background = "#BF616A80"          # redish for critical
foreground = "#ffffffff"
frame_color = "#ECEFF450"
timeout = 0

# Rules
[fullscreen_delay_everything]
fullscreen = delay

[fullscreen_show_critical]
msg_urgency = critical
fullscreen = show